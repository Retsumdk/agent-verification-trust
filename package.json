{
  "name": "agent-verification-trust",
  "description": "Agent verification and trust system with attestation, drift detection, and cross-session identity verification for autonomous AI agents",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "build": "tsc"
  },
  "dependencies": {
    "commander": "^11.1.0"
  },
  "devDependencies": {
    "bun-types": "^1.0.0",
    "typescript": "^5.3.0"
  }
}